# 간호사 노티 훈련 앱 — 전체 구조 설계

기존 청렴 훈련 앱과 같은 골격(카톡형 대화 → 선택지 → 피드백 → 종합 결과)을 쓰되,
노티 훈련에 맞게 **SBAR 채점 로직**을 추가한 구조입니다.

---

## 1. 파일 구조

vanilla HTML/CSS/JS + GitHub Pages 배포 기준으로, 지금까지 만드신 앱들과 동일한 패턴입니다.

```
notify-training/
├── index.html          # 시나리오 선택 화면
├── scenario.html        # 실제 채팅 진행 화면
├── result.html          # 종합 결과 화면
├── css/
│   └── style.css        # 카톡 UI 스타일 (말풍선, 프로필, 타임스탬프)
├── js/
│   ├── data.js           # 시나리오 데이터 (JSON)
│   ├── chat-engine.js     # 대화 진행 로직 (메시지 순차 출력, 텍스트 입력/전송 처리)
│   ├── scoring.js         # SBAR 채점 로직
│   └── state.js           # 진행 상태 관리 (localStorage)
└── assets/
    └── icons/            # 프로필 아이콘 등
```

---

## 2. 데이터 모델 (핵심)

시나리오를 코드에 하드코딩하지 않고 `data.js`에 JSON 형태로 분리 → 이후 시나리오 추가/수정이 쉬워짐.

```js
// data.js
const scenarios = [
  {
    id: "scn_01",
    title: "V/S 이상 소견 노티",
    subtitle: "야간 근무 중 혈압 저하 발견",
    partnerName: "당직의",
    partnerRole: "의사",
    messages: [
      { sender: "partner", text: "네, 무슨 일이세요?", time: "03:12" }
      // 상대방(의사) 메시지는 순차 등장
    ],
    context: {
      // 환자 상황 정보 - 선택지 판단에 필요한 배경
      patient: "802호 김OO (78세, F)",
      situation: "수축기 혈압 80까지 하강, 의식 저하 동반",
      vitals: { BP: "80/50", HR: "110", RR: "24", BT: "36.5", SpO2: "92%" }
    },
    // 선택지가 아니라 자유 텍스트 입력 방식으로 변경 (아래 2-1 참고)
    requiredElements: [
      {
        key: "환자식별",
        sbarCategory: "S",
        keywords: ["802호", "김OO", "김○○"],
        hint: "환자 확인이 되어야 의사가 어느 환자인지 알 수 있어요."
      },
      {
        key: "혈압수치",
        sbarCategory: "S",
        keywords: ["80/50", "혈압", "BP"],
        hint: "구체적인 활력징후 수치가 빠지면 심각도가 전달되지 않습니다."
      },
      {
        key: "의식상태",
        sbarCategory: "A",
        keywords: ["의식", "저하", "명료"],
        hint: "의식 수준은 상태 평가(A)의 핵심 요소입니다."
      },
      {
        key: "요청사항",
        sbarCategory: "R",
        keywords: ["수액", "처방", "요청", "봐주세요", "와주세요"],
        hint: "의사에게 무엇을 원하는지(R)가 빠지면 다음 행동이 불분명해집니다."
      }
    ]
  }
  // 시나리오 2~N...
];
```

**왜 선택지 대신 자유 텍스트인가**: 4지선다는 소거법으로 정답을 고를 수 있어 실제 노티 작성 능력을 훈련시키지 못합니다. 직접 문장을 작성하게 하고, 그 문장에 `requiredElements`의 키워드가 포함됐는지로 채점하면 "정답 고르기"가 아니라 "노티 구성하기"를 훈련시킬 수 있어요. 각 요소에 `sbarCategory`를 태그해두면 기존 SBAR 집계 로직과도 그대로 연결됩니다.

### 2-1. 채점 로직 (scoring.js)

```js
function gradeNotifyText(text, requiredElements) {
  const results = requiredElements.map(el => {
    const found = el.keywords.some(kw => text.includes(kw));
    return { key: el.key, sbarCategory: el.sbarCategory, found, hint: el.hint };
  });
  const passCount = results.filter(r => r.found).length;
  return {
    results,
    passCount,
    total: requiredElements.length,
    isPass: passCount === requiredElements.length  // 기준은 이후 조정 가능 (예: 80% 이상)
  };
}
```

결과는 "3/4 항목 포함 — 요청사항이 빠졌습니다" 처럼 **체크리스트형 피드백**으로 보여주는 걸 추천드려요. pass/fail만 보여주는 것보다 "뭘 놓쳤는지"가 학습 효과가 큽니다.

---

## 3. 화면 흐름

```
[index.html]
  시나리오 목록 (카드형: 제목 + 상황 한 줄 요약)
        │
        ▼
[scenario.html?id=scn_01]
  1) 상대방(의사) 메시지가 카톡처럼 순차 출력 (타이핑 효과 optional)
  2) 하단에 환자 상황 요약 카드 (청렴앱의 "협력기관 담당자" 말풍선 위 회색 정보처럼)
  3) 하단 텍스트 입력창에 노티 문장을 직접 작성 후 전송
  4) 전송 즉시 → 내 노티 메시지가 말풍선으로 표시됨
  5) 피드백 카드 (필수 요소 체크리스트: ✓환자식별 ✓상황 ✗요청사항 + SBAR 요소별 해설)
  6) "다음 시나리오" 버튼
        │
        ▼
[result.html]
  전체 시나리오 O/X 리스트 + 점수
  SBAR 항목별 누락 빈도 (예: "Recommendation 요청이 자주 누락되었습니다")
  "처음부터 다시하기"
```

---

## 4. 상태 관리 (state.js)

```js
// 세션 중 진행 상태를 localStorage에 저장
const state = {
  currentScenarioIndex: 0,
  answers: []  // { scenarioId, choiceId, sbarScore }
};

function saveAnswer(scenarioId, choiceId, sbarScore) {
  state.answers.push({ scenarioId, choiceId, sbarScore });
  localStorage.setItem("notifyTrainingState", JSON.stringify(state));
}
```

끝말잇기 앱에서 쓰신 것처럼 간단한 localStorage 기반이면 충분합니다. 로그인/서버 저장까지는 불필요해 보여요.

---

## 5. 채점 로직 (scoring.js)

```js
function calculateSBARSummary(answers) {
  const totals = { S: 0, B: 0, A: 0, R: 0 };
  answers.forEach(a => {
    totals.S += a.sbarScore.S;
    totals.B += a.sbarScore.B;
    totals.A += a.sbarScore.A;
    totals.R += a.sbarScore.R;
  });
  return totals; // 결과 화면에서 "어떤 요소가 약한지" 표시용
}
```

---

## 6. 사전 준비 단계 (차트 확인 → 전화하기)

기존 흐름 앞에 "준비 단계"를 하나 끼워 넣는 구조입니다. 핵심 긴장감: **전화 버튼은 언제든 누를 수 있지만, 준비 없이 누르면 의사 질문에 막힌다.**

### 6-1. 화면 흐름 변경 (수정: 탭 클릭 → 전체 펼침 / 화면 전환 → 이어붙임)

```
[scenario.html?id=scn_01]

  Step 1) 상황 발생 알림
    "802호 김OO님 낙상 발견" 같은 트리거 메시지 (상단 고정)

  Step 2) 환자 차트 — 6개 카테고리 전부 펼쳐서 한 화면에 나열
    V/S, 최근 Lab, 투약, I/O, 증상, 처치 → 클릭 없이 스크롤만으로 전체 확인 가능
    (탭/클릭 방식 폐기 — 실제 노티 시 정보를 다시 찾아봐야 하는 상황을 줄이기 위함)

  Step 3) "의사에게 전화하기" 버튼 클릭
    → 화면 전환 없이, 환자 차트 바로 아래에 대화창이 이어서 나타남
    → 차트는 계속 위쪽에 남아있어 스크롤해서 다시 볼 수 있음
    → 의사 메시지("네, 말씀하세요.") + 텍스트 입력창 등장

  Step 4) 노티 문장을 직접 작성해 전송 — 위 2-1 자유 텍스트 채점 방식 적용
    (차트가 항상 보이는 상태이므로, "확인 안 해서 답을 못하는" 상황은 사라짐
     → 이제 훈련 목표는 "정보를 아는가"가 아니라 "필요한 정보를 골라 문장으로 구성하는가"로 이동)

  Step 5) 피드백
    - 필수 요소 체크리스트: ✓환자식별 ✓상황 ✗요청사항 (+ 놓친 항목 hint)
    - SBAR 점수: 기존과 동일
```

> 참고: 기존 6-2/6-3에 있던 `requiredCategories`, `doctorQuestions`, "확인 안 하면 통화 실패" 로직은 이번 변경으로 **더 이상 필요하지 않습니다.** 차트가 항상 열려 있으니 "몰라서 못 답하는" 상황 자체가 없어지기 때문이에요. 대신 아래 데이터 모델도 그에 맞게 단순화합니다 (6-2 참고).
### 6-2. 데이터 모델 (단순화됨)

```js
// data.js — 시나리오 객체. requiredCategories/doctorQuestions 제거, chartData는 항상 전체 노출
{
  id: "scn_01",
  title: "낙상 발생 노티",
  trigger: "802호 김OO님 침상에서 낙상, 신규간호사인 당신이 발견",

  chartData: {
    VS: { BP: "128/76", HR: "88", RR: "18", BT: "36.7", SpO2: "97%" },
    Lab: { "최근 CBC": "정상범위", "최근 PT/INR": "1.4 (항응고제 복용중)" },
    Meds: ["와파린 5mg qd", "아스피린 100mg qd"],
    IO: { intake: "1200ml", output: "900ml" },
    Symptoms: "낙상 후 후두부 통증 호소, 의식 명료, 구토 없음",
    Treatment: "낙상 직후 활력징후 측정 완료, 냉찜질 적용"
  },

  // 통화 시작 후 자유 텍스트 입력 + 채점은 위 2-1의 requiredElements 구조 그대로 사용
  requiredElements: [ /* ... 위 2번 예시와 동일한 형식 ... */ ]
}
```

차트를 항상 다 보여주는 대신, `requiredElements`가 훈련의 핵심 역할을 그대로 가져갑니다 — 정보를 "알고 있는가"가 아니라, 알고 있는 정보 중 **무엇을 골라 노티 문장에 넣는가**를 채점하는 구조예요. 예를 들어 낙상 시나리오에서 항응고제 복용 여부는 차트에 보이지만, 그걸 노티 문장에 넣지 않으면 `requiredElements`의 "항응고제" 항목이 ✗로 체크됩니다.

### 6-3. 결과 화면

준비도(차트 확인 여부) 추적 로직은 제거되므로, 결과 화면은 기존 SBAR 점수 + requiredElements 체크리스트 집계만으로 구성합니다.

---

## 7. 확장 여지 (나중에 고려)

- **난이도 단계**: 신규 1개월차 / 3개월차용으로 시나리오 세트를 분리
- **타이머 요소**: 실제 응급 상황처럼 제한시간 내 선택하게 하면 긴장감 ↑ (단, 학습 초반엔 부담될 수 있어 선택적으로)
- **음성 노티 모드**: 회화 여권 앱에서 쓰신 STT를 응용해서, 텍스트가 아니라 실제 말로 노티하는 연습도 가능
- **관리자용 시나리오 추가 화면**: 교육팀에서 새 시나리오를 코드 수정 없이 추가하고 싶으시면 간단한 JSON 편집 폼도 고려 가능 (당장은 불필요할 수도)

---

## 8. Cursor 실행 프롬프트

지금까지 만들어진 뼈대(index.html)에 이어서, 아래 내용을 Cursor 채팅창에 그대로 붙여넣으시면 됩니다.

```
docs/architecture.md 문서 기준으로 scenario.html 흐름을 다음과 같이 수정해줘.

1. 기존에 선택지(버튼) 방식으로 만들려고 했던 노티 응답 부분을 자유 텍스트 입력 방식으로 바꿔줘.
   - 통화 연결 후 하단에 텍스트 입력창 + 전송 버튼을 추가
   - 전송하면 입력한 문장이 내 말풍선(우측)으로 표시됨

2. data.js의 각 시나리오 객체에 requiredElements 배열을 추가하는 구조로 바꿔줘.
   requiredElements의 각 항목은 { key, sbarCategory, keywords: [...], hint } 형태야.
   architecture.md의 "2-1. 채점 로직" 섹션에 있는 gradeNotifyText 함수 예시를 scoring.js에 그대로 구현해줘.

3. 텍스트 전송 시 gradeNotifyText로 채점해서, 아래 형태로 피드백 카드를 보여줘.
   - 체크리스트 형태: 각 requiredElements 항목별로 포함됐으면 ✓, 안 됐으면 ✗ 표시
   - 포함 안 된 항목은 hint 텍스트를 같이 보여줌
   - 상단에 "N/전체 항목 포함" 요약 표시

4. 지금은 시나리오 데이터가 부족하니, "낙상 발생 노티" 시나리오 하나를 architecture.md의 
   6-2 데이터 모델 예시(chartData, requiredCategories, doctorQuestions)와 
   2번 예시(requiredElements)를 합쳐서 완전한 형태로 data.js에 채워줘.

5. 다른 화면(index.html, result.html)은 지금 구조 그대로 두고, scenario.html과 data.js, scoring.js만 수정해줘.
```

이렇게 던지면 Cursor가 문서를 참고해서 (1) 입력창 UI (2) 데이터 구조 (3) 채점 로직 (4) 실제 시나리오 데이터까지 한 번에 반영해줄 거예요. 결과가 나오면 스크린샷으로 확인해서 다음 단계(사전 준비 단계인 차트 화면 연결) 넘어가면 됩니다.

---

## 다음 단계 제안

1. 아래 9번 프롬프트 실행 (차트 전체 노출 + 대화창 이어붙이기)
2. 낙상 외 다른 시나리오(V/S 이상, 코드블루, 배액량 이상 등) 콘텐츠 초안 추가로 작성

---

## 9. Cursor 실행 프롬프트 (UX 개선: 차트 상시 노출 + 대화창 이어붙이기)

```
docs/architecture.md 문서의 "6-1. 화면 흐름 변경" 섹션 기준으로 scenario.html을 다음과 같이 수정해줘.

1. 환자 차트를 탭 클릭 방식에서 전체 펼침 방식으로 바꿔줘.
   - V/S, 최근 Lab, 투약, I/O, 증상, 처치 6개 카테고리를 클릭 없이 처음부터 모두 펼쳐서 보여줘
   - 각 카테고리는 제목 + 내용으로 카드 형태로 세로 나열

2. "의사에게 전화하기" 버튼을 클릭해도 화면이 전환되지 않도록 바꿔줘.
   - 버튼 클릭 시, 환자 차트 카드들 바로 아래에 대화창 영역이 새로 나타남 (같은 페이지 안에서 이어붙임)
   - 차트는 계속 위쪽에 남아있어서 사용자가 스크롤해서 다시 볼 수 있어야 함
   - 대화창에는 의사의 첫 메시지("네, 말씀하세요.")와 텍스트 입력창 + 전송 버튼이 나타남

3. 버튼 클릭 후 화면이 자동으로 대화창 위치까지 스크롤되게 해줘 (부드러운 스크롤, scrollIntoView 등 활용).

4. 기존에 있던 requiredCategories, doctorQuestions 관련 로직(차트 확인 여부 추적, 통화 중 질문-답변 분기)은 제거해줘.
   차트가 항상 다 보이는 구조로 바뀌었으니 그 로직은 더 이상 필요 없어.

5. 텍스트 입력 → 전송 → gradeNotifyText 채점 → 체크리스트 피드백 표시 흐름은 지금 구조 그대로 유지해줘.
```

이 프롬프트로 실행하시면 화면이 "차트 전체 → (전화하기 클릭) → 차트 유지된 채 대화창 이어짐 → 노티 작성 → 피드백" 흐름으로 바뀔 거예요. 결과 스크린샷 주시면 다음 단계(다른 시나리오 콘텐츠) 이어서 진행하겠습니다.
