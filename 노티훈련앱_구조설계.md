# 간호사 노티 훈련 앱 — 전체 구조 설계

기존 청렴 훈련 앱과 같은 골격(카톡형 대화 → 선택지 → 피드백 → 종합 결과)을 쓰되,
노티 훈련에 맞게 **SBAR 채점 로직**을 추가한 구조입니다.

---

## 1. 파일 구조

vanilla HTML/CSS/JS + GitHub Pages 배포 기준으로, 지금까지 만드신 앱들과 동일한 패턴입니다.

```
notify-training/
├── index.html          # 시나리오 선택 화면
├── scenario.html        # 실제 채팅 진행 화면
├── result.html          # 종합 결과 화면
├── css/
│   └── style.css        # 카톡 UI 스타일 (말풍선, 프로필, 타임스탬프)
├── js/
│   ├── data.js           # 시나리오 데이터 (JSON)
│   ├── chat-engine.js     # 대화 진행 로직 (메시지 순차 출력, 선택지 렌더링)
│   ├── scoring.js         # SBAR 채점 로직
│   └── state.js           # 진행 상태 관리 (localStorage)
└── assets/
    └── icons/            # 프로필 아이콘 등
```

---

## 2. 데이터 모델 (핵심)

시나리오를 코드에 하드코딩하지 않고 `data.js`에 JSON 형태로 분리 → 이후 시나리오 추가/수정이 쉬워짐.

```js
// data.js
const scenarios = [
  {
    id: "scn_01",
    title: "V/S 이상 소견 노티",
    subtitle: "야간 근무 중 혈압 저하 발견",
    partnerName: "당직의",
    partnerRole: "의사",
    messages: [
      { sender: "partner", text: "네, 무슨 일이세요?", time: "03:12" }
      // 상대방(의사) 메시지는 순차 등장
    ],
    context: {
      // 환자 상황 정보 - 선택지 판단에 필요한 배경
      patient: "802호 김OO (78세, F)",
      situation: "수축기 혈압 80까지 하강, 의식 저하 동반",
      vitals: { BP: "80/50", HR: "110", RR: "24", BT: "36.5", SpO2: "92%" }
    },
    choices: [
      {
        id: "c1",
        text: "선생님, 802호 환자 혈압이 좀 낮아진 것 같아서요.",
        sbarScore: { S: 0, B: 0, A: 0, R: 0 },  // 정보 누락 다수
        feedback: "환자 식별 정보, 구체적 수치, 배경, 요청사항이 모두 빠져 있습니다."
      },
      {
        id: "c2",
        text: "802호 김OO님, 3분 전 BP 80/50, HR 110, 의식저하 있습니다. 수액 및 처방 요청드립니다.",
        sbarScore: { S: 1, B: 1, A: 1, R: 1 },  // SBAR 충족
        feedback: "환자 식별(S), 상황 배경(B), 평가(A), 요청(R)이 명확합니다. 적절한 노티입니다."
      }
      // 3~4개 선택지 권장
    ],
    correctChoiceId: "c2"
  }
  // 시나리오 2~N...
];
```

**SBAR 채점을 왜 이렇게 구조화하나**: 단순 정답/오답이 아니라 S(Situation)·B(Background)·A(Assessment)·R(Recommendation) 4개 항목을 선택지별로 0/1 채점해두면, 결과 화면에서 "어떤 요소가 자주 빠지는지" 개인별 피드백을 줄 수 있습니다. 신규간호사 교육 목적에는 이게 핵심이라고 생각해서 넣었어요.

---

## 3. 화면 흐름

```
[index.html]
  시나리오 목록 (카드형: 제목 + 상황 한 줄 요약)
        │
        ▼
[scenario.html?id=scn_01]
  1) 상대방(의사) 메시지가 카톡처럼 순차 출력 (타이핑 효과 optional)
  2) 하단에 환자 상황 요약 카드 (청렴앱의 "협력기관 담당자" 말풍선 위 회색 정보처럼)
  3) 선택지 3~4개 버튼 제시
  4) 선택 즉시 → 내 노티 메시지가 말풍선으로 표시됨
  5) 피드백 카드 (적절/부적절 + SBAR 요소별 해설)
  6) "다음 시나리오" 버튼
        │
        ▼
[result.html]
  전체 시나리오 O/X 리스트 + 점수
  SBAR 항목별 누락 빈도 (예: "Recommendation 요청이 자주 누락되었습니다")
  "처음부터 다시하기"
```

---

## 4. 상태 관리 (state.js)

```js
// 세션 중 진행 상태를 localStorage에 저장
const state = {
  currentScenarioIndex: 0,
  answers: []  // { scenarioId, choiceId, sbarScore }
};

function saveAnswer(scenarioId, choiceId, sbarScore) {
  state.answers.push({ scenarioId, choiceId, sbarScore });
  localStorage.setItem("notifyTrainingState", JSON.stringify(state));
}
```

끝말잇기 앱에서 쓰신 것처럼 간단한 localStorage 기반이면 충분합니다. 로그인/서버 저장까지는 불필요해 보여요.

---

## 5. 채점 로직 (scoring.js)

```js
function calculateSBARSummary(answers) {
  const totals = { S: 0, B: 0, A: 0, R: 0 };
  answers.forEach(a => {
    totals.S += a.sbarScore.S;
    totals.B += a.sbarScore.B;
    totals.A += a.sbarScore.A;
    totals.R += a.sbarScore.R;
  });
  return totals; // 결과 화면에서 "어떤 요소가 약한지" 표시용
}
```

---

## 6. 사전 준비 단계 (차트 확인 → 전화하기)

기존 흐름 앞에 "준비 단계"를 하나 끼워 넣는 구조입니다. 핵심 긴장감: **전화 버튼은 언제든 누를 수 있지만, 준비 없이 누르면 의사 질문에 막힌다.**

### 6-1. 화면 흐름 변경

```
[scenario.html?id=scn_01]

  Step 1) 상황 발생 알림
    "802호 김OO님 낙상 발견" 같은 트리거 메시지

  Step 2) 차트 화면
    V/S | 최근 Lab | 투약 | I/O | 증상 | 처치   ← 6개 탭/카드
    각 카드 클릭 → 상세 데이터 펼쳐짐 (열람 시 "확인함" 상태 기록)
    "의사에게 전화하기" 버튼은 처음부터 활성화 (준비 없이도 누를 수 있음 ← 일부러 막지 않음)

  Step 3) 전화 시뮬레이션 (통화 중 UI, 카톡과 다르게 상단에 "통화중 00:12" 타이머)
    의사가 질문 1~3개 던짐 (시나리오별로 필요한 카테고리에서 출제)
    - 해당 카테고리를 사전에 확인했다면 → 정확한 답변 선택지 제공
    - 확인 안 했다면 → "잠시만요, 확인하고 다시 걸겠습니다" / "음... 그건 확인 못했습니다" 만 선택 가능
      → 통화 강제 종료 또는 "다시 차트 확인하고 재통화" 로 분기

  Step 4) (통화 성공 시) SBAR 기반 최종 노티 문구 선택 — 기존 구조

  Step 5) 피드백
    - 준비도: 6개 카테고리 중 몇 개 확인했는지, 그 중 실제 필요했던 항목과 일치했는지
    - SBAR 점수: 기존과 동일
    - 종합 코멘트 예: "V/S는 확인하셨지만 최근 처치 내역을 놓쳐 의사 질문에 답하지 못했습니다."
```

### 6-2. 데이터 모델 확장

```js
// data.js — 시나리오 객체에 chartData / requiredCategories / doctorQuestions 추가
{
  id: "scn_01",
  title: "낙상 발생 노티",
  trigger: "802호 김OO님 침상에서 낙상, 신규간호사인 당신이 발견",

  chartData: {
    VS: { BP: "128/76", HR: "88", RR: "18", BT: "36.7", SpO2: "97%" },
    Lab: { "최근 CBC": "정상범위", "최근 PT/INR": "1.4 (항응고제 복용중)" },
    Meds: ["와파린 5mg qd", "아스피린 100mg qd"],
    IO: { intake: "1200ml", output: "900ml" },
    Symptoms: "낙상 후 후두부 통증 호소, 의식 명료, 구토 없음",
    Treatment: "낙상 직후 활력징후 측정 완료, 냉찜질 적용"
  },

  // 이 시나리오에서 "진짜 필요한" 카테고리 — 항응고제 복용 중이므로 Lab/Meds가 핵심
  requiredCategories: ["VS", "Lab", "Meds", "Symptoms"],

  doctorQuestions: [
    {
      category: "Meds",
      question: "이 환자 항응고제 드시고 계신가요?",
      ifChecked: { answerText: "네, 와파린 복용 중입니다.", ok: true },
      ifNotChecked: { answerText: "확인 후 다시 전화드리겠습니다.", ok: false }
    },
    {
      category: "Symptoms",
      question: "의식 상태랑 두통 호소는 어때요?",
      ifChecked: { answerText: "의식 명료하고 후두부 통증 호소하고 있습니다.", ok: true },
      ifNotChecked: { answerText: "다시 확인해보겠습니다.", ok: false }
    }
  ],

  // 기존 SBAR 선택지 구조는 통화 성공 이후 그대로 이어짐
  choices: [ /* ... 기존과 동일 ... */ ]
}
```

**설계 포인트**: `requiredCategories`를 6개 전부가 아니라 시나리오마다 다르게(예: 낙상엔 항응고제 여부가 핵심, 발열엔 최근 Lab의 WBC/CRP가 핵심) 넣어두면 "무조건 다 확인하면 만점"이 아니라 **상황에 맞는 판단력**을 훈련시킬 수 있어요. 이게 실제 임상에서 신규들이 어려워하는 지점과 가장 가깝다고 봅니다 — 뭘 봐야 하는지 자체를 모르는 것.

### 6-3. 상태/채점 로직 추가

```js
// state.js에 추가
function checkChartCategory(scenarioId, category) {
  state.checkedCategories = state.checkedCategories || {};
  state.checkedCategories[scenarioId] = state.checkedCategories[scenarioId] || [];
  if (!state.checkedCategories[scenarioId].includes(category)) {
    state.checkedCategories[scenarioId].push(category);
  }
}

// scoring.js에 추가
function calculatePreparedness(scenario, checkedCategories) {
  const required = scenario.requiredCategories;
  const checked = required.filter(c => checkedCategories.includes(c));
  return {
    ratio: checked.length / required.length,
    missed: required.filter(c => !checkedCategories.includes(c))
  };
}
```

결과 화면에서는 "SBAR 점수" 옆에 "사전 준비도" 항목을 하나 더 두고, 종합 결과에서 자주 놓치는 카테고리(예: "투약 정보를 5번 중 3번 놓쳤습니다")를 보여주면 교육적으로 특히 유용할 것 같아요.

---

## 7. 확장 여지 (나중에 고려)

- **난이도 단계**: 신규 1개월차 / 3개월차용으로 시나리오 세트를 분리
- **타이머 요소**: 실제 응급 상황처럼 제한시간 내 선택하게 하면 긴장감 ↑ (단, 학습 초반엔 부담될 수 있어 선택적으로)
- **음성 노티 모드**: 회화 여권 앱에서 쓰신 STT를 응용해서, 텍스트가 아니라 실제 말로 노티하는 연습도 가능
- **관리자용 시나리오 추가 화면**: 교육팀에서 새 시나리오를 코드 수정 없이 추가하고 싶으시면 간단한 JSON 편집 폼도 고려 가능 (당장은 불필요할 수도)

---

## 다음 단계 제안

1. SBAR 기반 시나리오 3~5개 초안(대사 + 선택지 + 해설) 같이 작성
2. 또는 카톡 UI CSS/HTML 뼈대부터 먼저 잡기

어느 쪽부터 하시겠어요?
